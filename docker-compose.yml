version: '3.9'
services:
  vlp-ic-api:
    build: .
    ports:
      - "8082:80"
    volumes:
      - ./app:/usr/src/app
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]